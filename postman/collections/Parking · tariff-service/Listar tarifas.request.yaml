$kind: http-request
description: GET /v1/tariffs -> 200 con todas las tarifas, activas o no.
url: "{{baseUrlTariff}}/v1/tariffs"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('200 OK', () => pm.response.to.have.status(200));
    language: text/javascript
order: 4000
